spring:
  application:
    name: Spring Boot Application with MongoDB, OpenAPI, JUnit and Docker

  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: user_db
      port: 27017
      host: localhost

logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter:  DEBUG

server:
  port: 8080
  servlet:
    context-path: /
